# A Minimalist Blog system built by Python

## How to use?
1. Install Python
2. Install dependencies `pip`:
    ```shell
    pip install -r requirements.txt
    ```
3. Run main.py:
    ```shell
    python main.py
    ```